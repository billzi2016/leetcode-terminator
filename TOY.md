# 这是一个玩具 (虽然这个文档是我让ai写的,但不是ai的想法,是我的吐槽)

请不要把这个项目部署到生产环境。如果你这么做了，后果自负。

---

## 缺了什么

打开这个项目的代码，你会发现它几乎没有任何生产级别的东西：

**安全**
- 没有 CSRF 保护（Django 的 CSRF middleware 没有启用在 API 层）
- 没有认证，没有 JWT，没有 Session，没有 IAM
- 没有防火墙规则，没有速率限制，没有 IP 白名单
- 没有 Admin 后台权限管理
- `DEBUG=True` 在默认配置里

**架构**
- 没有数据库，数据全在内存和文件系统里
- 更不要说分库分表、读写分离、Sharding
- 没有消息队列，没有任务调度，没有异步 Worker
- 没有缓存层（Redis/Memcached）
- 没有独立前端，HTML 直接从 Django Template 出来

**可靠性**
- 没有负载均衡，单进程跑着
- 来个并发峰值直接爆
- 没有限流，Ollama 被打满了就卡死
- 没有熔断，没有重试，没有降级
- 没有健康检查终端（只有一个简单的 `/api/health/`）

**部署**
- 没有 K8s，没有 Helm Chart，没有自动扩缩容
- 没有 CI/CD pipeline
- 没有日志聚合，没有 APM，没有告警
- Ollama 硬绑定本地，无法横向扩展

**这个列表可以继续写很长。**

---

## 那它有什么用

它有且只有一个用途：**感受 AI 的能力边界。**

不到半小时，从一张白纸到一个跑得起来的完整工具——翻译、题解、流式输出、Markdown 渲染、侧边栏筛选、持久化缓存。这件事本身就是结论。

---

## 这不是 Vibe Coding

Vibe Coding 是另一回事：打开 AI，随便说几句，看它生成什么，觉得能跑就算完，完全不知道代码在做什么。

这个项目不是。

这里每一行代码背后都有明确的技术决策：为什么用文件而不是数据库、为什么 SSE 而不是 WebSocket、为什么 Ninja 而不是 DRF、Prompt 怎么设计才能让模型输出结构化内容。AI 是执行层，判断是人做的。你不理解这些，AI 给你的只是能跑的垃圾。

玩具归玩具，地基不能是毁的。
